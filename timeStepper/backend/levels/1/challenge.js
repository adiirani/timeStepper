// This JavaScript code takes an input string and applies the transformation for Level 1 (Reverse).
// It expects a variable 'inputString' and will output the reversed version.

function transform(inputString) {
    return inputString.split('').reverse().join('');
  }
  
  // Example input: "abc123"
  // Example output: "321cba"
  